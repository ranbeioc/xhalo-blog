# Production Live-write Readiness Checklist

> Status: Pending Approval  
> Verdict: Pending  

---

## 1. Pre-execution Verification Checklist

This checklist must be verified by the operator *immediately* prior to executing the production live-write trial request.

| Check | Item | Verification Method | Status |
|---|---|---|---|
| **1.1** | Staging dry-run/shadow/PR evidence complete | Verify `docs/production-pr-trial-evidence-20260614.md` exists and is marked as passed. | ⏳ |
| **1.2** | Main branch protection | Verify `main` branch protection is enabled on the target repository `ranbeioc/hexo-blog` to prevent direct push. | ⏳ |
| **1.3** | GitHub App/Token permissions | Verify the configured GITHUB_TOKEN has write access only to `ranbeioc/hexo-blog`. | ⏳ |
| **1.4** | Cloudflare Access configured | Confirm Access Zerotrust JWT validation is active and returning `401` for unauthenticated requests. | ⏳ |
| **1.5** | Turnstile validation active | Confirm `TURNSTILE_SECRET_KEY` is configured on the worker. | ⏳ |
| **1.6** | Disaster Recovery / Rollback runbook | Confirm operator has `docs/production-rollback-runbook.md` open and is familiar with commands. | ⏳ |
| **1.7** | Monitoring active | Confirm Cloudflare Log Stream or tailing is active (`wrangler tail --env production` or Dashboard logs open). | ⏳ |
| **1.8** | Owner Approval statement | Confirm owner has signed the live-write approval record. | ⏳ |

---

## 2. Controlled Live-write Trial Parameters

| Parameter | Approved Value | Verification Method | Status |
|---|---|---|---|
| **2.1** | Maximum request count | **1** | Enforced by operator. | ⏳ |
| **2.2** | Mode | `live` | Request payload mode parameter. | ⏳ |
| **2.3** | Target repository | `ranbeioc/hexo-blog` | Env `GITHUB_REPO` value. | ⏳ |
| **2.4** | Target branch | `draft/production-live-write-trial-20260614` | Autogenerated branch. | ⏳ |
| **2.5** | Post slug | `production-live-write-trial-20260614` | Post parameter. | ⏳ |
| **2.6** | Post title | `"Production Live-write Trial"` | Post parameter. | ⏳ |

---

## 3. Post-Execution Cleanup Checklist

This checklist must be verified *immediately* after the live-write request has been processed.

| Check | Item | Expected Behavior | Status |
|---|---|---|---|
| **3.1** | Confirm PR creation | Pull Request is successfully opened on `ranbeioc/hexo-blog` by the Worker. | ⏳ |
| **3.2** | Confirm task completion | Task status transitions to `completed` in D1 `tasks` table. | ⏳ |
| **3.3** | Confirm audit log | D1 audit logs show `draft_publish_queued` and `draft_publish_completed`. | ⏳ |
| **3.4** | PR Close/Merge status | Pull Request remains open (or is merged if specifically approved by owner). *Note: For the live-write trial, we may choose to merge it to verify end-to-end publish, but this must be explicitly authorized.* | ⏳ |
| **3.5** | Restore safe state | `LIVE_WRITES_ENABLED` is restored to `false` on the production API worker. | ⏳ |
| **3.6** | Redeploy validation | Redeployment confirmed via Wrangler. Subsequent writing requests return `403`. | ⏳ |

---

## 4. Final Sign-off

* **Operator Sign-off**:  
  Signature: `____________________` (Operator Name)  
  Date: `____________________`  

* **Owner Sign-off**:  
  Signature: `____________________` (Repository Owner)  
  Date: `____________________`  
